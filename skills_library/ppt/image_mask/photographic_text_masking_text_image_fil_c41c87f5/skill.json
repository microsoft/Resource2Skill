{
  "skill_id": "photographic_text_masking_text_image_fil_c41c87f5",
  "skill_name": "Photographic Text Masking (Text Image Fill)",
  "domain": "ppt",
  "category": "image_mask",
  "source": {
    "type": "youtube",
    "video_url": "https://www.youtube.com/watch?v=qwOqPo8liqM",
    "video_id": "qwOqPo8liqM",
    "video_title": "How To Mask Text in PowerPoint (FAST & EASY) | 100% WORKS | NO Yapping! | Text Mask in PowerPoint",
    "channel": "Guide Hub (2026 WORKING GUIDES)",
    "duration_sec": 133,
    "views": 13
  },
  "extracted_at": "2026-04-17T15:51:59.149186",
  "analysis": "### 1. High-level Design Pattern Extraction\n\n> **Skill Name**: Photographic Text Masking (Text Image Fill)\n\n* **Core Visual Mechanism**: The defining visual idea is using bold, heavy typography as a clipping mask for a high-quality photograph or texture. Instead of a solid color, the text is filled with the image, turning the letterforms into \"windows\" that reveal the picture underneath. \n\n* **Why Use This Skill (Rationale)**: This technique creates a highly editorial, magazine-like aesthetic. It bridges the gap between text and imagery, making the typography itself the primary visual hook. It forces the audience to engage with both the written message and the visual context simultaneously, reducing slide clutter by merging two elements into one.\n\n* **Overall Applicability**: This style shines in high-impact scenarios:\n  - Title slides for presentations and product launches\n  - Section dividers / Chapter headers\n  - Portfolio hero pages\n  - Motivational quotes or single-keyword impact slides (e.g., \"GROWTH\", \"INNOVATION\")\n\n* **Value Addition**: Compared to plain text next to an image, text masking elevates the slide from a standard corporate layout to a bespoke graphic design piece. It adds immediate production value and visual sophistication without requiring complex animations.\n\n\n### 2. Visual Breakdown\n\n* **Step A: Core Visual Elements**\n  - **Typography**: Requires ultra-bold, thick, \"display\" fonts (e.g., Impact, Arial Black, Futura Extra Bold). Thin fonts fail because they do not provide enough surface area to reveal the image.\n  - **Image**: High-contrast, vibrant, or textured photography works best.\n  - **Color Logic**: The background is typically kept minimalist (e.g., pure white `(255, 255, 255, 255)` or stark black `(0, 0, 0, 255)`) to ensure the masked text pops. The \"colors\" come entirely from the embedded image.\n  - **Text Hierarchy**: Usually limited to 1-5 words. This is a display technique, not meant for body paragraphs.\n\n* **Step B: Compositional Style**\n  - **Spatial Feel**: Centered, dominant, and monumental.\n  - **Proportions**: The text block should occupy at least 70-80% of the slide's width and 40-60% of its height to ensure the image within is recognizable.\n\n* **Step C: Dynamic Effects & Transitions**\n  - In PowerPoint, this is achieved natively via `Shape Format > Merge Shapes > Intersect`.\n  - While the video doesn't show complex animations, this effect pairs perfectly with a slow \"Grow/Shrink\" animation on the underlying image, making the photo slowly pan or zoom *inside* the static text.\n\n\n### 3. Reproduction Code\n\n#### 3a. Implementation Method Selection\n\n| Aspect of the effect | Method | Why this method |\n|---|---|---|\n| **Text Masking / Boolean Intersect** | **PIL/Pillow** | `python-pptx` cannot natively execute PowerPoint's \"Merge Shapes\" Boolean operations. Injecting OOXML picture fills into text is highly unstable across PPT versions. PIL perfectly rasterizes the masked image. |\n| **Image Retrieval** | `urllib.request` | Used to grab dynamic stock photography mimicking the \"Insert > Stock Images\" step. |\n| **Slide Layout** | `python-pptx` native | Places the final generated transparent PNG precisely on the slide canvas. |\n\n> **Feasibility Assessment**: 95%. The code perfectly reproduces the final visual effect shown in the tutorial. The only difference is that the resulting masked text is an image rather than a native editable text box, which is a necessary programmatic tradeoff since standard PPTX APIs lack boolean geometry engines.\n\n#### 3b. Complete Reproduction Code\n\n```python\ndef create_slide(\n    output_pptx_path: str,\n    title_text: str = \"HOW TO MASK\\nTEXT IN\\nPOWERPOINT\",\n    bg_palette: str = \"bird,nature\",  # Keyword for stock image\n    **kwargs,\n) -> str:\n    \"\"\"\n    Create a PPTX file reproducing the 'Photographic Text Masking' visual effect.\n    \"\"\"\n    from pptx import Presentation\n    from pptx.util import Inches\n    from PIL import Image, ImageDraw, ImageFont\n    import urllib.request\n    import io\n    import os\n\n    # 1. Initialize Presentation\n    prs = Presentation()\n    prs.slide_width = Inches(13.333)\n    prs.slide_height = Inches(7.5)\n    slide = prs.slides.add_slide(prs.slide_layouts[6])  # Blank layout\n\n    # Setup dimensions (1920x1080 for 16:9)\n    img_w, img_h = 1920, 1080\n\n    # 2. Fetch Background Image (with fallback)\n    try:\n        # Using a reliable stock image service via keyword\n        url = f\"https://image.pollinations.ai/prompt/{bg_palette.replace(',', '%20')}?width=1920&height=1080&nologo=true\"\n        req = urllib.request.Request(url, headers={'User-Agent': 'Mozilla/5.0'})\n        with urllib.request.urlopen(req) as response:\n            base_img = Image.open(io.BytesIO(response.read())).convert(\"RGBA\")\n    except Exception as e:\n        print(f\"Image download failed, using gradient fallback. Error: {e}\")\n        # Fallback: Create a vibrant gradient if network fails\n        base_img = Image.new('RGBA', (img_w, img_h))\n        draw_base = ImageDraw.Draw(base_img)\n        for y in range(img_h):\n            r = int(20 + (230 * (y / img_h)))\n            g = int(50 + (100 * (y / img_h)))\n            b = int(180 + (50 * (y / img_h)))\n            draw_base.line([(0, y), (img_w, y)], fill=(r, g, b, 255))\n\n    # Resize background to strictly match canvas\n    base_img = base_img.resize((img_w, img_h), Image.Resampling.LANCZOS)\n\n    # 3. Create the Text Alpha Mask\n    # L mode (8-bit pixels, black and white)\n    mask = Image.new(\"L\", (img_w, img_h), 0)  # Start completely black (transparent)\n    draw_mask = ImageDraw.Draw(mask)\n\n    # Robust Font Loading: Try to find a heavy, bold font\n    font_options = [\n        \"impact.ttf\", \"Impact.ttf\", \n        \"arialbd.ttf\", \"Arial Bold.ttf\", \n        \"trebucbd.ttf\", \"tahoma.ttf\", \n        \"DejaVuSans-Bold.ttf\"\n    ]\n    \n    font = None\n    # Dynamically find the best font size (Start huge and shrink)\n    font_size = 350\n    \n    # Try loading fonts\n    for font_name in font_options:\n        try:\n            font = ImageFont.truetype(font_name, font_size)\n            break\n        except IOError:\n            continue\n            \n    if not font:\n        font = ImageFont.load_default()\n        print(\"Warning: Heavy fonts not found. Using default font.\")\n\n    # Format text (add newlines if single long string)\n    if \"\\n\" not in title_text and len(title_text) > 15:\n        words = title_text.split()\n        title_text = \"\\n\".join([\" \".join(words[i:i+2]) for i in range(0, len(words), 2)])\n\n    # Scale down font size to fit within 90% of width / 80% of height\n    if hasattr(font, \"getbbox\"):\n        while font_size > 50:\n            bbox = draw_mask.multiline_textbbox((0, 0), title_text, font=font, align=\"center\")\n            text_w = bbox[2] - bbox[0]\n            text_h = bbox[3] - bbox[1]\n            if text_w < img_w * 0.9 and text_h < img_h * 0.8:\n                break\n            font_size -= 10\n            try:\n                font = ImageFont.truetype(font.path, font_size)\n            except:\n                break\n\n    # Calculate centered position\n    bbox = draw_mask.multiline_textbbox((0, 0), title_text, font=font, align=\"center\")\n    text_w = bbox[2] - bbox[0]\n    text_h = bbox[3] - bbox[1]\n    x = (img_w - text_w) // 2\n    y = (img_h - text_h) // 2\n\n    # Draw white text on black mask (White = keep image, Black = transparent)\n    draw_mask.multiline_text((x, y), title_text, font=font, fill=255, align=\"center\")\n\n    # 4. Composite Image and Mask\n    # Create a completely transparent canvas\n    transparent_bg = Image.new(\"RGBA\", (img_w, img_h), (0, 0, 0, 0))\n    # Apply the base image onto the transparent canvas using the text as the alpha mask\n    final_composite = Image.composite(base_img, transparent_bg, mask)\n\n    # 5. Save and Insert to Slide\n    temp_img_path = \"temp_text_mask.png\"\n    final_composite.save(temp_img_path, format=\"PNG\")\n\n    # Insert image taking up the full slide\n    slide.shapes.add_picture(temp_img_path, Inches(0), Inches(0), Inches(13.333), Inches(7.5))\n\n    # Clean up temp file\n    if os.path.exists(temp_img_path):\n        os.remove(temp_img_path)\n\n    # Save presentation\n    prs.save(output_pptx_path)\n    return output_pptx_path\n```",
  "frames": [
    {
      "path": "frame_00_stage_20pct.jpg",
      "seconds": 26,
      "description": "stage_20pct"
    },
    {
      "path": "frame_01_stage_40pct.jpg",
      "seconds": 53,
      "description": "stage_40pct"
    },
    {
      "path": "frame_02_stage_60pct.jpg",
      "seconds": 79,
      "description": "stage_60pct"
    },
    {
      "path": "frame_03_stage_80pct.jpg",
      "seconds": 106,
      "description": "stage_80pct"
    }
  ]
}