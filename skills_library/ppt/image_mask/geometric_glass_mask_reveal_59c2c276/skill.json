{
  "skill_id": "geometric_glass_mask_reveal_59c2c276",
  "skill_name": "Geometric Glass-Mask Reveal",
  "domain": "ppt",
  "category": "image_mask",
  "source": {
    "type": "youtube",
    "video_url": "https://www.youtube.com/watch?v=m-LHud-BbCU",
    "video_id": "m-LHud-BbCU",
    "video_title": "How to Make a Creative Thank You Slide in Microsoft PowerPoint",
    "channel": "Knowledge for Future",
    "duration_sec": 331,
    "views": 128107
  },
  "extracted_at": "2026-04-17T04:02:52.413536",
  "analysis": "# Agent_Skill_Distiller: Pattern Extraction & Implementation\n\n### 1. High-level Design Pattern Extraction\n\n> **Skill Name**: Geometric Glass-Mask Reveal\n\n* **Core Visual Mechanism**: The defining visual idea is the use of a continuous, semi-opaque geometric \"mask\" overlaid on a full-screen photograph. By carving complex, transparent, intersecting geometric shapes (rounded diamonds and circles) out of a gradient layer, the background photo is selectively revealed. Floating vector outlines dynamically intersect the cutouts, creating depth and a sophisticated, multi-layered architectural aesthetic.\n\n* **Why Use This Skill (Rationale)**: This technique creates a distinct \"windowing\" effect that commands focus while still allowing rich, textured background imagery to exist without overwhelming the text. It breaks the monotony of standard rectangular grids and introduces a modern, dynamic, and forward-looking energy suitable for high-impact visual statements.\n\n* **Overall Applicability**: This pattern is exceptional for:\n  - **Closing/Title Slides**: \"Thank You\", \"Welcome\", or \"Vision\" slides.\n  - **Tech & Architecture Presentations**: The intersecting lines and geometric precision align well perfectly with engineering, software, and architectural content.\n  - **Portfolio Covers**: Showcasing a hero image in a highly stylized, non-generic way.\n\n* **Value Addition**: Transforms a basic full-screen photo into a bespoke, agency-level graphic. It adds immense visual depth (background photo + gradient cutout mask + floating neon outlines + 3D reflected text) that standard PowerPoint layouts cannot achieve easily.\n\n\n### 2. Visual Breakdown\n\n* **Step A: Core Visual Elements**\n  - **Background Layer**: A high-contrast, wide-angle photograph (e.g., cityscape or landscape).\n  - **Gradient Mask Layer**: A full-screen overlay with custom cutout \"windows.\"\n    - *Gradient Logic*: Warm-to-cool diagonal linear gradient (e.g., Peach `(255, 204, 153, 230)` to Soft Blue `(153, 204, 255, 230)`).\n  - **Floating Outlines**: Thin geometric strokes matching the cutouts but offset slightly in angle or position.\n    - *Colors*: Cyan `(0, 255, 255, 255)`, Yellow `(255, 255, 0, 255)`, White `(255, 255, 255, 255)`.\n  - **Typography**: Heavy, bold, all-caps text with a physical presence (using outer shadow and floor reflection).\n\n* **Step B: Compositional Style**\n  - **Spatial Logic**: Radial/Diagonal symmetry. The primary diamond acts as a massive central focal point (occupying ~50% of the slide height). Smaller shapes radiate outwards along perfect 45-degree and 135-degree axes.\n  - **Layering**: Photograph (Z=0) → Cutout Mask (Z=1) → Vector Outlines (Z=2) → Typography (Z=3).\n\n* **Step C: Dynamic Effects & Transitions**\n  - *Code Reproducible*: The complex image compositing, the drop shadows, and the text reflections.\n  - *PowerPoint Native (Manual)*: Spin animations applied to the floating vector outlines, and a pulse animation on the text block.\n\n\n### 3. Reproduction Code\n\n#### 3a. Implementation Method Selection\n\n| Aspect of the effect | Method | Why this method |\n|---|---|---|\n| **Geometric Cutout Mask** | `PIL/Pillow` | Native `python-pptx` cannot perform boolean \"Subtract\" shape operations. We must programmatically generate an alpha-channel mask and composite it over the background photo. |\n| **Floating Offset Outlines** | `PIL/Pillow` | Best integrated smoothly into the background compositing step, allowing perfect geometric alignments and sub-pixel antialiasing. |\n| **Floor Reflection & Shadow**| `lxml` XML Injection | PowerPoint's native text reflection and shadow parameters are inaccessible via the basic `python-pptx` API, requiring direct OOXML modification. |\n| **Typography & Layout** | `python-pptx` | Used to place the final editable text elements over the composited background. |\n\n> **Feasibility Assessment**: 95%. The visual structure, masking, compositing, gradient overlays, and text effects are perfectly reproducible. The only elements omitted are the PowerPoint animation timeline steps (spin/pulse), as the focus is on generating the static visual asset.\n\n#### 3b. Complete Reproduction Code\n\n```python\ndef create_slide(\n    output_pptx_path: str,\n    title_text: str = \"THANK\\nYOU\",\n    bg_theme: str = \"cityscape,architecture\",\n    **kwargs,\n) -> str:\n    \"\"\"\n    Create a PPTX file reproducing the 'Geometric Glass-Mask Reveal' visual effect.\n    \"\"\"\n    import os\n    import math\n    import requests\n    from io import BytesIO\n    from pptx import Presentation\n    from pptx.util import Inches, Pt\n    from pptx.dml.color import RGBColor\n    from pptx.enum.text import PP_ALIGN\n    from pptx.oxml import parse_xml\n    from pptx.oxml.ns import nsdecls\n    from PIL import Image, ImageDraw, ImageFilter\n\n    # --- 1. Initialization ---\n    prs = Presentation()\n    prs.slide_width = Inches(13.333)\n    prs.slide_height = Inches(7.5)\n    slide = prs.slides.add_slide(prs.slide_layouts[6])\n    \n    WIDTH, HEIGHT = 1920, 1080\n\n    # --- 2. Image Download & Preparation ---\n    def get_background_image(theme: str) -> Image.Image:\n        try:\n            # Using picsum for reliable high-res random imagery\n            url = f\"https://picsum.photos/1920/1080?{theme}\"\n            res = requests.get(url, timeout=10)\n            res.raise_for_status()\n            img = Image.open(BytesIO(res.content)).convert(\"RGBA\")\n            return img.resize((WIDTH, HEIGHT), Image.LANCZOS)\n        except Exception as e:\n            print(f\"Image download failed: {e}. Using fallback solid color.\")\n            return Image.new(\"RGBA\", (WIDTH, HEIGHT), (20, 30, 50, 255))\n\n    bg_img = get_background_image(bg_theme)\n\n    # --- 3. Create Gradient Base for Mask ---\n    color1 = (255, 180, 140)  # Peach/Orange\n    color2 = (140, 180, 220)  # Light Blue\n    gradient_img = Image.new(\"RGB\", (WIDTH, HEIGHT), color1)\n    top_img = Image.new(\"RGB\", (WIDTH, HEIGHT), color2)\n    grad_mask = Image.new(\"L\", (WIDTH, HEIGHT))\n    \n    # Generate diagonal gradient data\n    for y in range(HEIGHT):\n        for x in range(WIDTH):\n            # Diagonal gradient distribution\n            val = int(255 * (x + y) / (WIDTH + HEIGHT))\n            grad_mask.putpixel((x, y), val)\n            \n    gradient_img = Image.composite(gradient_img, top_img, grad_mask).convert(\"RGBA\")\n    \n    # Add slight transparency to the overall gradient\n    gradient_img.putalpha(240)\n\n    # --- 4. Punching Transparent Geometric Holes ---\n    # We draw black (0) on a white (255) mask to punch holes.\n    alpha_mask = Image.new(\"L\", (WIDTH, HEIGHT), 255)\n\n    def draw_rotated_rrect_hole(mask: Image.Image, cx, cy, w, h, radius, angle):\n        temp_size = int(max(w, h) * 1.5)\n        # Create a local mask: 0 inside the shape, 255 outside\n        shape_mask = Image.new(\"L\", (temp_size, temp_size), 255)\n        draw = ImageDraw.Draw(shape_mask)\n        x0, y0 = (temp_size - w) / 2, (temp_size - h) / 2\n        draw.rounded_rectangle([x0, y0, x0+w, y0+h], radius=radius, fill=0)\n        # Rotate the shape\n        shape_mask = shape_mask.rotate(angle, resample=Image.BICUBIC, expand=False, fillcolor=255)\n        \n        # Paste the hole onto the main alpha_mask\n        paste_x = int(cx - temp_size / 2)\n        paste_y = int(cy - temp_size / 2)\n        \n        # We want to paste black (0) wherever shape_mask is black (0)\n        # Inverting shape_mask so white represents the hole for the `paste` mask parameter\n        inv_shape_mask = Image.eval(shape_mask, lambda p: 255 - p)\n        black_patch = Image.new(\"L\", (temp_size, temp_size), 0)\n        mask.paste(black_patch, (paste_x, paste_y), mask=inv_shape_mask)\n\n    # Define the geometry of the holes\n    holes = [\n        # Center huge diamond\n        {\"cx\": 960, \"cy\": 540, \"w\": 550, \"h\": 550, \"r\": 60, \"a\": 45},\n        # Top-Left diagonal\n        {\"cx\": 580, \"cy\": 160, \"w\": 180, \"h\": 180, \"r\": 20, \"a\": 45},\n        {\"cx\": 400, \"cy\": -20, \"w\": 120, \"h\": 120, \"r\": 15, \"a\": 45},\n        # Bottom-Right diagonal\n        {\"cx\": 1340, \"cy\": 920, \"w\": 250, \"h\": 250, \"r\": 30, \"a\": 45},\n        {\"cx\": 1560, \"cy\": 1140, \"w\": 150, \"h\": 150, \"r\": 15, \"a\": 45},\n        # Bottom-Left diagonal\n        {\"cx\": 580, \"cy\": 920, \"w\": 200, \"h\": 200, \"r\": 25, \"a\": 45},\n        {\"cx\": 380, \"cy\": 1120, \"w\": 130, \"h\": 130, \"r\": 15, \"a\": 45},\n        # Top-Right diagonal\n        {\"cx\": 1340, \"cy\": 160, \"w\": 200, \"h\": 200, \"r\": 25, \"a\": 45},\n    ]\n\n    # Draw all diamond holes\n    for h in holes:\n        draw_rotated_rrect_hole(alpha_mask, h[\"cx\"], h[\"cy\"], h[\"w\"], h[\"h\"], h[\"r\"], h[\"a\"])\n\n    # Draw circle holes directly\n    c_draw = ImageDraw.Draw(alpha_mask)\n    circles = [\n        {\"cx\": 350, \"cy\": 250, \"r\": 80},\n        {\"cx\": 1550, \"cy\": 750, \"r\": 90},\n    ]\n    for c in circles:\n        c_draw.ellipse([c[\"cx\"]-c[\"r\"], c[\"cy\"]-c[\"r\"], c[\"cx\"]+c[\"r\"], c[\"cy\"]+c[\"r\"]], fill=0)\n\n    # Apply the hole mask to the gradient layer\n    gradient_img.putalpha(alpha_mask)\n    \n    # Composite over background\n    bg_img.paste(gradient_img, (0, 0), gradient_img)\n\n    # --- 5. Draw Floating Outlines ---\n    def draw_floating_outline(bg: Image.Image, cx, cy, w, h, radius, angle, color, width):\n        temp_size = int(max(w, h) * 1.6)\n        temp_img = Image.new(\"RGBA\", (temp_size, temp_size), (0, 0, 0, 0))\n        draw = ImageDraw.Draw(temp_img)\n        x0, y0 = (temp_size - w) / 2, (temp_size - h) / 2\n        \n        if radius > 0:\n            draw.rounded_rectangle([x0, y0, x0+w, y0+h], radius=radius, outline=color, width=width)\n        else:\n            # Circle\n            draw.ellipse([x0, y0, x0+w, y0+h], outline=color, width=width)\n            \n        temp_img = temp_img.rotate(angle, resample=Image.BICUBIC, expand=False)\n        paste_x, paste_y = int(cx - temp_size / 2), int(cy - temp_size / 2)\n        bg.paste(temp_img, (paste_x, paste_y), temp_img)\n\n    # Floating vector accents\n    draw_floating_outline(bg_img, 960, 540, 620, 620, 70, 45, (255, 255, 0, 255), 8)   # Yellow diamond\n    draw_floating_outline(bg_img, 960, 540, 650, 600, 80, 20, (255, 255, 255, 220), 4) # White skewed rectangle\n    draw_floating_outline(bg_img, 960, 540, 700, 700, 0, 0, (0, 255, 200, 255), 5)     # Cyan circle\n    \n    # Save composite to file and insert into slide\n    composite_path = \"temp_composite_bg.png\"\n    bg_img.save(composite_path)\n    slide.shapes.add_picture(composite_path, 0, 0, Inches(13.333), Inches(7.5))\n\n    # --- 6. Typography & Styling (lxml Injection) ---\n    tx_width = Inches(8)\n    tx_height = Inches(3)\n    tx_left = (prs.slide_width - tx_width) / 2\n    tx_top = (prs.slide_height - tx_height) / 2\n\n    textbox = slide.shapes.add_textbox(tx_left, tx_top, tx_width, tx_height)\n    tf = textbox.text_frame\n    tf.word_wrap = True\n    tf.vertical_anchor = PP_ALIGN.CENTER\n    \n    p = tf.paragraphs[0]\n    p.alignment = PP_ALIGN.CENTER\n    p.text = title_text\n    \n    # Text formatting\n    for run in p.runs:\n        font = run.font\n        font.name = \"Impact\"  # Heavy display font\n        font.size = Pt(85)\n        font.bold = True\n        font.color.rgb = RGBColor(255, 255, 255)\n        \n        # Inject Reflection and Drop Shadow via lxml\n        rPr = run._r.get_or_add_rPr()\n        effect_xml = f\"\"\"\n        <a:effectLst xmlns:a=\"http://schemas.openxmlformats.org/drawingml/2006/main\">\n            <!-- Drop Shadow -->\n            <a:outerShdw blurRad=\"40000\" dist=\"35000\" dir=\"2700000\" algn=\"tl\" rotWithShape=\"0\">\n                <a:srgbClr val=\"000000\">\n                    <a:alpha val=\"60000\"/>\n                </a:srgbClr>\n            </a:outerShdw>\n            <!-- Floor Reflection -->\n            <a:reflection blurRad=\"10000\" stA=\"50000\" endA=\"500\" endPos=\"35000\" dist=\"0\" dir=\"5400000\" sy=\"-100000\" algn=\"bl\" rotWithShape=\"0\"/>\n        </a:effectLst>\n        \"\"\"\n        effect_element = parse_xml(effect_xml)\n        rPr.append(effect_element)\n\n    # Save and Cleanup\n    prs.save(output_pptx_path)\n    if os.path.exists(composite_path):\n        os.remove(composite_path)\n        \n    return output_pptx_path\n```",
  "frames": [
    {
      "path": "frame_00_stage_20pct.jpg",
      "seconds": 66,
      "description": "stage_20pct"
    },
    {
      "path": "frame_01_stage_40pct.jpg",
      "seconds": 132,
      "description": "stage_40pct"
    },
    {
      "path": "frame_02_stage_60pct.jpg",
      "seconds": 198,
      "description": "stage_60pct"
    },
    {
      "path": "frame_03_stage_80pct.jpg",
      "seconds": 264,
      "description": "stage_80pct"
    }
  ]
}