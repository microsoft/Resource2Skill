# growth_pct

## Description

Period-over-period percentage growth: (current - prior) / prior

## Parameters

```json
{
  "example": "=(B3-B2)/B2",
  "pattern": "=({current}-{prior})/{prior}",
  "result_format": "percent_signed"
}
```