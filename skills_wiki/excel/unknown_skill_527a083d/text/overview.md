Skip — The core mechanism of inserting and dynamically linking text in arbitrary drawing shapes (rectangles, ovals) to cell values, as demonstrated in the video, is not directly supported by the `openpyxl` library. `openpyxl` focuses on cell data, formatting, charts, and images, but does not provide an API for creating or manipulating these types of drawing objects with dynamic text links as shown.