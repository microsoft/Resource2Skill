# running_total

## Description

Cumulative sum from row 2 down — anchor first cell of range, leave end relative

## Parameters

```json
{
  "example": "=SUM($B$2:B5)",
  "pattern": "=SUM($B$2:{cur})",
  "result_format": "thousand_sep"
}
```